Load a specialized skill when the task at hand matches one of the skills listed in the system prompt.

Use this tool to inject the skill's instructions and resources into current conversation. The output may contain detailed workflow guidance as well as references to scripts, files, etc in the same directory as the skill.

The skill name must match one of the skills listed in your system prompt.

If the tool returns a capability fallback instead of an installed skill, follow the fallback as capability/software planning guidance. Do not retry alternative skill aliases for the same request.

For file-based tasks, if the user supplied an explicit absolute path, validate and use that path first. Do not use glob/search before checking the explicit path. Use glob/search only after the explicit path is missing, inaccessible, or ambiguous.
